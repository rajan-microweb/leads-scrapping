# Database
DATABASE_URL="postgresql://user:password@localhost:5432/leads_scrapping?schema=public"

# NextAuth
AUTH_SECRET="your-secret-key-here"
AUTH_URL="http://localhost:3000"

# You can generate AUTH_SECRET by running: openssl rand -base64 32

# Email Configuration (for password reset)
# Leave these empty for development mode (emails will be logged to console)
# For production, configure with your SMTP provider (Gmail, SendGrid, AWS SES, etc.)
# See SMTP-CONFIGURATION-GUIDE.md for detailed setup instructions
SMTP_HOST=""                    # e.g., "smtp.gmail.com" or "smtp.sendgrid.net"
SMTP_PORT="587"                 # Usually 587 (TLS) or 465 (SSL)
SMTP_USER=""                    # Your email or "apikey" for SendGrid
SMTP_PASSWORD=""                # App password or API key (see guide for details)
SMTP_FROM="noreply@example.com" # The sender email address
APP_URL="http://localhost:3000" # Your application URL
