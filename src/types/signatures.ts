export type Signature = {
  id: string
  userId: string
  name: string
  content: string
  createdAt: string
  updatedAt: string
}

export type SignatureFormValues = {
  name: string
  content: string
}
